# Public Worker base URL only (no R2 secret keys). Edit after deployment.
# For production, prefer Firebase Remote Config and pass the URL into [R2Service].
R2_WORKER_URL=https://your-worker.workers.dev
