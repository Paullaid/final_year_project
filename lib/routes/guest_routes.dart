/// Route names for the unauthenticated [Navigator] stack (see [AuthGate]).
abstract final class GuestRoutes {
  static const welcome = '/welcome';
  static const login = '/login';
  static const signIn = '/sign-in';
}
